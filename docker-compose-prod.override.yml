version: "2"

services:
  nginx:
    volumes:
      - ./nginx-prod.conf:/etc/nginx/conf.d/default.conf
      - /etc/letsencrypt/live/mautic.mylegalfidelity.com:/etc/letsencrypt/live/mautic.mylegalfidelity.com
    command: |
      "exec nginx -g 'daemon off;'"
